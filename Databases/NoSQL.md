
## Key Value Store 

The key value store generally allows for o(1) reads and write and is often backed by memory or SSD. Data stores can maintain keys in lexicographic order, allowing efficient retrieval of key ranges. Key-value store can allow for storing of metadata with a value. 


## Document Store

A document store is centered around documents where a document stores all information for a given object. 